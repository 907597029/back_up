class Solution:
    def reverseWords(self, s: str) -> str:
        s.split(" ")
        print(s)
        # for(i in rang(len(str))):
        #     string[i] = string[i]

import sklearn
from sklearn import datasets

# s = datasets.